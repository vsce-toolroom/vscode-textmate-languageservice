.shared_windows_runners:
  tags:
  - shared-windows
  - windows
  - windows-1809

variables:
  GIT_SUBMODULE_STRATEGY: recursive
  GIT_SUBMODULE_DEPTH: 0

cache:
  paths:
  - node_modules/

stages:
- test

test:
  extends:
  - .shared_windows_runners
  stage: test
  script:
  - npm install
  - npm test
  artifacts:
    paths:
    - test/data/
